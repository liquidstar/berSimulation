classdef Evaluator
    properties
        
    end
    
    methods
        function eval = Evaluator(dataSource, ofdmVariant, transmitter, channel, receiver)
            % Determine Average PAPR by transmit symbol
            % Determine Bit Error Rate
            % Maybe plot some impressive curves and shit.
            
            % src_bits = comm.dataSource.serialBits;rx_bits = comm.receiver.serRecBits(1:length(src_bits));
            % rx_bits ~= src_bits
        end
    end
end